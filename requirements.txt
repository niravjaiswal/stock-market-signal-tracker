# Core
httpx>=0.27
anthropic>=0.40
python-dotenv>=1.0
rich>=13.7
feedparser>=6.0
beautifulsoup4>=4.12

# Optional: live press-conference transcription (only needed if ENABLE_LIVE_TRANSCRIPTION=1)
# Install separately:  pip install yt-dlp faster-whisper
# yt-dlp>=2024.0
# faster-whisper>=1.0

# Dev
pytest>=8.0
